<% if @order.errors.any? || @order_item.errors.any? %>
	alert("<%= @order_item.errors.full_messages.to_sentence %>");
<% else %>
	// document.getElementById("shopping_cart").innerHTML = "";
	// document.getElementById('shopping_cart').insertAdjacentHTML('afterbegin', '<%#= j render("shared/cart") %>');
	document.getElementById("shopping_cart").innerHTML = "<%= j render('shared/cart') %>";
	var addedToCartModal = new bootstrap.Modal(document.getElementById('addedToCart'));
	addedToCartModal.show();
	setTimeout(function(){
	  addedToCartModal.hide();
	}, 1500);
<% end %>
